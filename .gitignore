# Backup runtime data: keep the folder, never commit its contents (contain live auth.json/meta.json)
data_backup/*
!data_backup/.gitkeep

# Logs
*.log

# Python caches / artifacts
__pycache__/
*.py[cod]
.pytest_cache/
